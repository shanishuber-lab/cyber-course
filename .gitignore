# ── קורסים ──────────────────────────────────────────────────
# כל קורס הוא ריפו בפני עצמו, עם היסטוריה ונכסים משלו.
# הריפו הזה הוא סביבת העבודה: הוראות, מדריכים, סוכנים ונכסי מותג.
# תוכן קורס לא נכנס לכאן, גם כשעוד אין לו ריפו מרוחק.
courses/

# ── פלט בנייה ────────────────────────────────────────────────
output/

# ── פייתון ──────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/

# ── מערכת ההפעלה ────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store

# ── שארית תיקייה כפולה שנוצרה מנתיב שגוי ─────────────────────
"course creator"/
